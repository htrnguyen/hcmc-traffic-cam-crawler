# ========================================
# HCMC Traffic Camera Crawler - Environment Configuration
# Copy this file to .env and update with your values:
# cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
# ========================================

# ========================================
# Crawler Settings (has defaults, can be left as-is)
# ========================================
INTERVAL_SEC=180 # 3 minutes
CONCURRENCY=200
TIMEOUT_S=8.0
RETRIES=2

# ========================================
# Storage Settings (has defaults, can be left as-is)
# ========================================
OUT_ROOT=staging_images
LOG_DIR=logs

# ========================================
# Cloud Storage (Optional but recommended)
# ========================================
# Rclone remote name - CAN BE ANY NAME you choose (gdrv, myremote, gdrive, etc.)
# IMPORTANT: Must match the name used in: .\rclone.exe config create [YOUR_NAME] drive scope=drive
RCLONE_REMOTE=gdrv

# Google Drive folder ID where images will be uploaded
# Get this from your Google Drive folder URL: https://drive.google.com/drive/folders/[FOLDER_ID]
GDRIVE_FOLDER_ID=your_actual_folder_id_here

# Path to rclone executable
RCLONE_EXE=.\rclone-v1.71.0-windows-amd64\rclone.exe

# Sync pacing 
RCLONE_MIN_AGE_S=30
RCLONE_PERIOD_S=60
